<Page x:Class="Sudarikov3ISP1113.View.PageUser"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Sudarikov3ISP1113.View"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageUser">

    <Grid>
        <ListView x:Name="listViewPet" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Имя" Width="100" DisplayMemberBinding="{Binding LastName}" />
                    <GridViewColumn Header="Фамилия" Width="100" DisplayMemberBinding="{Binding FirstName}" />
                    <GridViewColumn Header="Номер" Width="100" DisplayMemberBinding="{Binding Patronymic}" />
                    <GridViewColumn Header="Пол" Width="100" DisplayMemberBinding="{Binding Phone}" />
                    <GridViewColumn Header="Возраст" Width="150" DisplayMemberBinding="{Binding Email}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>